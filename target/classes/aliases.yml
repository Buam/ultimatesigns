# A list of strings for each variable which can be used instead of that variable (useful if variables don't fit on signs because of their length)
aliases:
  player:
    - p
  displayname:
    - dn
  world:
    - w
  balance:
    - b